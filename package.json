{
  "name": "@cutie/tool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn workspace @cutie/web3d-entry dev",
    "build": "yarn workspace @cutie/web3d-entry build",
    "lint": "yarn workspaces foreach -A run lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.1.1"
}
